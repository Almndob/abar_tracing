# Deny access to all files in this directory and subdirectories
<FilesMatch "^\.ht">
    Order allow,deny
    Deny from all
    Satisfy All
</FilesMatch>

# Deny all access to this folder
Order Deny,Allow
Deny from all
